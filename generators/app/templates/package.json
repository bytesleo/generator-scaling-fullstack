{
  "name": "<%= name %>",
  "description": "<%= name %>",
  "repository": "",
  "license": "MIT",
  "scripts": {
    "start": "concurrently --raw <%- npm.start %>",
    "install": "concurrently --raw <%- npm.install %>",
    "build": "concurrently --raw <%- npm.build %>",
    "test": "concurrently --raw <%- npm.test %>"
  },
  "devDependencies": {
    "concurrently": "^3.1.0"
  }
}
